---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-api
  namespace: docblock
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-api
  template:
    metadata:
      labels:
        app: admin-api
    spec:
      imagePullSecrets:
        - name: pigbenjamin-ghcr-secret
      containers:
        - name: admin-api
          image: ghcr.io/pigbenjamin/docblock-rag-platform/admin-api:latest
          ports:
            - containerPort: 8765
          envFrom:
            - configMapRef:
                name: docblock-config
            - secretRef:
                name: docblock-db-secret
            - secretRef:
                name: docblock-app-secret
          volumeMounts:
            - name: ingest-data
              mountPath: /data
      volumes:
        - name: ingest-data
          persistentVolumeClaim:
            claimName: docblock-ingest-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: admin-api
  namespace: docblock
spec:
  type: NodePort
  selector:
    app: admin-api
  ports:
    - port: 8765
      targetPort: 8765
      nodePort: 31765
